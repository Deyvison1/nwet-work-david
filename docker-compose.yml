version: '3.7'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
volumes:
  mysql-data:
networks:
  springboot-mysql-network:
    name: springboot-mysql-network